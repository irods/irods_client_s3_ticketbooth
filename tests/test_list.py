import pytest

#@pytest.mark.parametrize(('path', 'expected_permission'), (
#    ('/create?collection=/tempZone/home/kory', 'read'),
#    ('/create?collection=/tempZone/home/kory&permission=write', 'write')
#))

#def test_list(client):
#    response = client.post(path, headers={'authorization': 'Basic a29yeTpyb2Rz'})
#    assert response.status_code == 200
#    assert len(response.data) > 0
#
#    response = client.post('/list', headers={'authorization': 'Basic a29yeTpyb2Rz'})
#    assert response.status_code == 200
#    assert '"permission":"{}"'.format(expected_permission) in str(response.data)

